# A안 90일 실행계획 — 판정 정밀도 심화

작성일: 2026-08-05 · 기준: `reports/기준체크_경쟁분석_리포트.md` (5개사 조사, 2026-08-05)
선행 문서: [실행 우선순위](./04-실행-우선순위.md) · [다음작업 타임라인](./02-다음작업-타임라인.md) · [검증기록](./03-검증기록.md)

---

## 0. 이 문서의 위치

경쟁 분석에서 세 개의 전략안(A 판정 정밀도 심화 / B 인접 도구 확장 / C 기준 충돌 해설)을
비교한 결과 **A안을 채택했다.** 이 문서는 A안의 실행 계획이다.

A안과 B안은 같은 주 20시간을 놓고 상충한다. **둘 다 하면 둘 다 반쯤 된다.**
B안은 채택하지 않으므로 이 90일 동안 신규 계산기를 만들지 않는다.

---

## 1. 전략 명제

> 도구 개수로는 이미 졌다. 기준체크가 이길 수 있는 유일한 자리는
> **피부양자 판정 한 주제에서 관계 분기의 정밀도와 사례 단위 대조 기록을 동시에 갖는 것**이다.

포지셔닝: **좁은 범위 × 최고 검증 깊이** (좌상단)

넓은 범위 × 깊은 검증(우상단)은 비어 있지만 **기회가 아니라 무덤**이다.
세금핏이 그 자리에 못 간 이유는 능력이 아니라 경제성이다 — 56개 도구에 각각 사례 대조를
붙이는 비용이 도구 하나 더 만드는 유입 이득보다 크다. 회계사가 운영하는 곳이 못 가는 자리를
주 20시간 개인이 갈 수 없다.

### 왜 A안이 이기는가

경쟁사의 **유인 구조** 때문이다.

- **세금핏**: 자기 검증 현황판에 56개 도구 중 39개가 "상세 근거 미등록"이라고 공개했다.
  피부양자 자격 점검도 그 39개에 들어 있다. 56개를 굴리는 운영자에게 도구 하나에
  사례 대조 20건을 붙이는 것은 항상 후순위다.
- **건보계산기**: 사례 대조 프로세스 자체가 없다. `data-sources`는 출처 링크 목록이다.
- **유리지갑**: 판정 도구가 아니라 콘텐츠다.

### 하지 않을 것

**도구 개수 경쟁.** 세금핏 56개, 유리지갑 20개 이상을 쫓지 않는다.

---

## 2. 실행 순서가 바뀐 이유 — 반론 테스트 결과

A안을 세운 뒤 "경쟁사가 이걸 어떻게 무력화할까"를 세 가지로 검토했다.
세 번째 반론이 **실행 순서를 바꿨다.**

| 반론 | 성립하는가 | 대응 |
|---|---|---|
| ① 세금핏이 피부양자 도구 하나에만 사례 대조 20건 + KICPA 배지를 얹는다 | **성립.** 방어 불가 | 관계 분기 깊이 + 누적 대조 건수를 **함께** 쌓아 복제 비용을 두 배로. 하나만으로는 못 막는다 |
| ② 세금핏이 관계·동거·혼인 분기를 추가한다 | **성립.** 한 달이면 가능 | 관계 분기를 **유일한** 차별점으로 삼지 않는다 |
| ③ 세금핏이 아무것도 안 한다 | **가장 가능성 높고, 우리에게 가장 위험** | 그들은 유입이 있고 우리는 색인조차 미확인이다. 가만히 있어도 우리가 색인 안 되면 진다 |

**③ 때문에 0–30일 첫 항목이 색인 실측이다.** 검증 깊이는 유입이 있어야 의미가 있다.
검증 깊이를 먼저 파고 색인을 나중에 보는 순서는 5주를 쓰고 아무 변화가 없는 결말로 간다.

### A안의 구조적 약점 — 그리고 보완

**검증 깊이는 클릭하기 전에 보이지 않는다.** 검색 결과에 "공단 사례 20건과 대조했습니다"는
노출되지 않는다. 이 약점을 두 가지로 보완한다.

1. 61–90일에 **사례 기록표를 구조화 데이터로 노출** — 검증 자산을 검색에 보이는 형태로 변환
2. 31–60일에 **C안의 안전한 형태**(기준 설명 가이드)를 배치 — 검색 유입 경로를 만들되
   경쟁사 실명 저격은 하지 않는다

즉 **A안 단독으로는 위험하고, C안 일부를 A안의 노출 장치로 쓰는 조합이 실제 계획이다.**

---

## 3. 0–30일 (2026-08-05 ~ 09-04)

### 3-1. 색인 상태 실측 — 최우선

반론 ③ 때문에 이것이 첫 번째다. 다른 작업보다 먼저 착수한다.

- [x] Google Search Console에서 색인 대상 라우트의 **색인 상태** 실측
      — 2026-08-06 **11개 전부 색인 확인** (`site:` 2페이지 + GSC URL 검사 교차)
- [ ] 네이버 서치어드바이저 동일 확인 — 미착수
- [x] 미색인 URL의 제외 사유 분류 — **미색인 URL 없음.** 1차 측정의 `when-voluntary-
      continuation-wins` 미색인 판정은 측정 오류였고 GSC로 정상 확인됨
- [~] 홈의 http·https 중복 색인 — **2026-08-06 진단 완료. 설정 결함 없음.**
      canonical(절대 https)·http 절대링크 0건·`curl -I` 308 Permanent Redirect 모두 정상.
      원인은 2026-08-03 도메인 연결 직후의 낡은 크롤 스냅샷 하나다.
      **남은 조치는 GSC에서 http URL에 색인 생성 요청 → 2주 뒤 통합 확인.**
      상세: `docs/06-QA-전수점검.md` 「관찰 ②」
- [ ] 임시 Vercel 주소가 검색 결과에 남아 있는지 확인
- [x] 결과를 `docs/06-QA-전수점검.md`에 날짜·URL과 함께 기록 — 2026-08-06 「Q4-1. 색인 실측」

**2026-08-06 실측 결과, 이 절의 중단 조건은 발동하지 않는다.** 색인 대상 11개가 전부
확인되어 7절 1번(색인 5개 미만 + 기술적 결함) 시나리오는 해제됐다. **A안을 계속한다.**

색인이 병목이 아니라는 것이 확인됐으므로, 다음 병목은 **순위와 클릭**이다.
GSC 실적 보고서의 검색어·노출·평균게재순위를 3-4 이후 작업의 판단 근거로 쓴다.

**중단 조건**: 색인이 16개 중 5개 미만이고 원인이 기술적 결함이면,
3-2 이후를 멈추고 색인 문제부터 해결한다. 검증 깊이는 유입 없이는 자산이 아니다.

### 3-2. 지역보험료 `재대조 전` 표기 제거

우리 포지셔닝 전체가 검증 깊이인데, 사이트에서 가장 잘 보이는 자리에
"아직 확인 안 됨"이 적혀 있다. **정직함을 지불하고 아무것도 사지 못하는 상태**다.

- [x] 금융소득 기준 수정 이후의 공단 모의계산 재대조 (대표 사례 13건, 2026-08-05)
- [x] `src/lib/premium/regional.ts`의 `VERIFIED_AGAINST_NHIS` 상태 갱신
- [x] `/verification-policy/` 표의 "재대조 전 참고 계산" → "검증된 참고 계산"
- [x] 도구 상단 `TrustSignal` 문구 동기화
- [x] `docs/03-검증기록.md`에 대조일·입력값·기대값·실제값·오차 기록

### 3-3. 배우자 동반(부부 합산) 판정 구현

**경쟁 분석에서 발견된 공백이었다. 2026-08-05 구현으로 보완했다.**
현재 `/verification-policy/`는 기혼 피부양자의 배우자 소득·재산 요건을 별도로 확인하는
모델과 그 한계를 설명한다.

- [x] 국민건강보험법 시행규칙 제2조·별표 1의2에서 **배우자 요건의 정확한 법적 근거 확인**
      — 세금핏 화면을 근거로 삼지 않는다. 그들의 구현이 맞는지 검증하지 않았다
- [x] `src/lib/dependent/types.ts`에 배우자 소득·재산 입력 추가
- [x] `judge.ts`에 부부 동반 판정 분기 추가 (순수 함수 유지)
- [x] 경계값 단위 테스트 추가 (배우자 소득 2,000만원 전후)
- [x] `DependentJudge.tsx`에 조건부 입력 (혼인 = 기혼일 때만 노출)
- [x] `/verification-policy/` 근거 매핑 표에서 해당 한계 문구 제거

### 3-4. 검증 원칙 페이지를 사례 기록표로 승격

현재 `/verification-policy/`는 **방법론**을 설명한다. 경쟁사도 방법론은 쓸 수 있다.
차별점은 **개별 사례의 숫자**다.

- [ ] `docs/03-검증기록.md`의 대조 사례를 공개 가능한 형태로 정리
- [ ] `/verification-policy/`에 표 추가: `입력값 요약 | 기대값 | 우리 결과 | 오차 | 대조일 | 출처`
- [ ] 대조 실패·불일치 사례도 숨기지 않고 원인과 함께 표시

---

## 4. 31–60일 (09-05 ~ 10-04)

### 4-1. 관계 분기 확장

반론 ②에 대한 대응은 "관계 분기만으로 방어하지 않는다"이지만,
관계 분기의 **깊이**는 여전히 복제 비용을 올린다.

- [ ] 손자녀·외손자녀의 **부모 부양능력 요건** 추가
- [ ] 이혼·사별 상태 처리 (현재 "공단 확인 필요"로 회피 중)
- [ ] 기혼 피부양자의 배우자 재산 요건 (3-3의 소득 요건 다음 단계)
- [ ] 각 분기마다 근거 조항 매핑 + 경계값 테스트

### 4-2. 사적연금 기준 충돌 해소 — C안의 안전한 형태

**경쟁 분석에서 발견된 기준 충돌이다.**

- 세금핏 FAQ: "공적연금과 사적연금이 **모두** 소득합계에 포함됩니다"
  (입력 항목도 "연금소득 구간 (공적+사적)")
- 기준체크 가이드: "공적연금은 합산되고 **개인연금은 제외**됩니다"

**둘 중 하나는 틀렸고, 이건 판정 결과를 바꾼다.**

- [ ] 시행규칙 별표 1의2 원문 + 공단 안내로 **우리 입장부터 재확인**
      — 우리가 맞다고 믿는 것과 확인한 것은 다르다
- [ ] 확인되면 `연금소득이 피부양자 자격에 미치는 영향` 가이드를
      **독립 근거 문서 수준으로 보강** (조문 인용 + 공적/사적 구분 표 + 판정 예시)
- [ ] 확인 결과 **우리가 틀렸다면** 코드·가이드·검증기록을 즉시 정정하고
      `docs/03-검증기록.md`에 정정 이력을 남긴다

**금지 사항**: 경쟁사를 실명으로 지목하는 비교 콘텐츠를 만들지 않는다.
평판 리스크가 크고, 상대가 조용히 고치면 콘텐츠가 죽는다.
**기준 자체를 설명하고 근거를 밝히는 형태로만 쓴다.**

### 4-3. 대조 사례 20건 이상으로 확대

- [ ] 지역보험료 대조 사례를 13건 → 20건 이상으로
- [ ] 피부양자 자체 재현 8건에 배우자 동반 판정 사례 추가
- [ ] 공단 로그인 모의계산의 임의입력 제약 우회 경로 재탐색
      (`MEMORY.md` 남은 운영 작업 5)

### 4-4. 11월 재산정 랜딩 초안

> **주의**: "11월 검색 폭증"은 `docs/archive/00-주제-리서치.md`에서 이미 근거 없음으로
> 정정된 가정이다. 3-1의 GSC 데이터로 **실제 검색 추이를 확인한 뒤** 투입 규모를 정한다.
> 데이터가 뒷받침하지 않으면 이 항목은 축소하거나 뺀다.

---

## 5. 61–90일 (10-05 ~ 11-03)

- [ ] **사례 기록표를 구조화 데이터로 노출** — A안 최대 약점(검증 깊이가 클릭 전에 안 보임)의
      핵심 보완. 어떤 스키마가 유효한지 Rich Results Test로 먼저 검증한 뒤 적용
- [ ] 11월 시즌 랜딩 배포 및 내부 링크 연결 (4-4에서 근거가 확인된 경우에만)
- [ ] 경쟁 3사 검증 현황 재점검 (세금핏 39개 미등록 수치가 줄었는지가 핵심 관찰 지표)

### 5-1. 애드센스

`docs/02`·`docs/04`에 흩어져 있던 애드센스 항목을 여기로 흡수했다 (2026-08-05 정리).
**신청은 90일 구간 끝에 둔다.** 검증 자산이 완성되기 전에 광고를 붙이면 신뢰가 먼저 깎인다.

신청 전 조건 (전부 충족해야 신청)

- [ ] 주요 가이드가 **실제로 색인**되고 최신 출처·최종 확인일을 갖춘다 (3-1 결과에 의존)
- [ ] 개인정보처리방침·이용약관·문의 페이지가 실제 운영 방식과 일치한다
- [ ] 광고·쿠키 사용을 개인정보처리방침에 반영한다
- [ ] 광고가 입력 폼 위·결과 핵심 문장 사이에 들어가지 않는 배치안을 만든다

승인 후 원칙

- [ ] 자동광고를 무조건 켜지 않는다. 가이드 본문과 **결과 이후**에만 제한적으로 배치
- [ ] 모바일 CLS와 판정 완료율을 광고 전후로 비교
- [ ] 수익보다 이탈률·사용성·신뢰 훼손 여부를 우선 확인

### 5-2. 90일 이후 재검토 항목 (지금 하지 않는다)

- 국민연금 조기·연기수령, 육아휴직급여 등 **신규 도구** — B안이며 채택하지 않았다.
  90일 종료 후 선행 지표 결과를 보고 다시 판단한다.
- 다음 가이드 주제 선정 — Search Console 노출·클릭·질의와 판정 완료 이벤트가
  누적된 뒤에 정한다. 3-1 이전에는 판단 근거가 없다.

---

## 6. 선행 지표

전략이 작동하는지 **30일 안에** 알려주는 신호다. 매출·광고 수익은 후행 지표라 제외했다.

| 지표 | 현재 | 30일 목표 | 확인 방법 |
|---|---|---|---|
| `재대조 전` 표기가 남은 도구 수 | **0** | **0** | `/verification-policy/` 표 |
| 색인 완료 URL / 색인 대상 11 | **11 / 11** (2026-08-06, Google) | **유지 11 / 11** · 네이버 실측 | GSC · 네이버 서치어드바이저 |
| 공개된 공단 대조 사례 건수 | 13 (재대조 완료) | **20+** | `/verification-policy/` 사례 표 |

보조 관찰 지표 (목표 설정 없이 추이만):

- `judge_start` → `judge_complete` 전환율 (GA4)
- 세금핏 검증 현황판의 "범위 한정" 도구 수 (현재 39 — 줄어들면 위협 신호)

### 6-1. 검색 실적 기준선 (2026-08-06)

색인이 병목이 아님이 확인된 뒤의 다음 관찰 대상이다.
**출발점으로만 쓰고, 이 숫자로 전략을 판단하지 않는다.**

| 지표 | 2026-08-03 (1일치) | 2026-09-04 판단 시점 |
|---|---|---|
| 노출 | **4** | 추이 확인 |
| 클릭 | 0 | 추이 확인 |
| 평균 게재순위 | 6.2 | 추이 확인 |
| 노출된 페이지 수 | 2 (가이드만) | 도구까지 확대되는가 |
| GSC 검색어 노출 | 없음(익명화 기준 미만) | **질의가 보이기 시작하는가** |

**가장 중요한 관찰:** 노출된 2개가 전부 가이드(`losing-eligibility`,
`property-tax-base`)이고 도구 3개는 0회다. 이 패턴이 9월에도 유지되면
**가이드 → 도구 내부 링크가 전환의 실제 경로**라는 뜻이므로, 4-1·4-2의
우선순위 판단에 반영한다.

상세와 해석 한계는 `docs/06-QA-전수점검.md` 「관찰 ④」에 있다.

---

## 7. 이 계획을 중단해야 하는 조건

계획을 세울 때 중단 조건을 함께 적어 둔다. 아니면 매몰비용으로 계속하게 된다.

1. **색인이 30일 안에 회복되지 않고 원인이 기술적 결함**이면 → A안 중단, 색인 문제 우선
2. **사적연금 기준에서 우리가 틀린 것으로 확인**되면 → 정정이 모든 것보다 우선.
   신뢰가 자산인 서비스에서 오류 방치는 전략 실패보다 치명적이다
3. **세금핏이 피부양자 도구에 사례 대조를 등록**하면 → A안의 전제가 무너진다. 재검토
4. **90일 시점에 색인·유입이 0에 가까우면** → 검증 깊이가 아니라 유통 채널이 문제다.
   B안이 아니라 유통(커뮤니티·제휴)을 재검토한다

---

## 8. 미확인 전제 (이 계획이 딛고 선 가정)

이 계획은 다음 가정 위에 있다. **사실이 아니라 가정이다.**

1. 경쟁 4개사의 실제 트래픽·검색 순위·전환율은 확인하지 않았다.
   "세금핏이 이미 유입이 있다"는 도구 수와 색인 상태에서 추론한 것이다.
2. "11월 검색 집중"은 근거 없음으로 이미 정정된 가정이다.
3. 세금핏의 배우자 동반 판정 구현이 **법적으로 맞는지** 검증하지 않았다.
4. 유리지갑에서 관찰한 서술 오류 3건은 우리 판정 로직 기준의 관찰이며,
   법령 원문 대조 전에는 콘텐츠로 인용하지 않는다.

상세 근거와 반론은 `reports/기준체크_경쟁분석_리포트.md` 7절을 본다.
