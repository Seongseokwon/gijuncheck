# 사대보험 (sabohum)

소득이 바뀔 때 내 사회보험이 어떻게 바뀌는지 **판정**해주는 도구.

경쟁 사이트는 전부 "계산"만 합니다. 이 프로젝트의 유일한 차별점은 **판정**입니다.
설계 판단이 흔들릴 때는 이 문장을 기준으로 결정하세요.

## 문서

| 파일 | 내용 |
|---|---|
| [docs/00-주제-리서치.md](docs/00-주제-리서치.md) | 15개 후보 조사, 경쟁 사이트 검증, 주제 선정 근거 |
| [docs/01-상세기획서.md](docs/01-상세기획서.md) | 정보구조, 판정 트리, SEO 설계, 수익화, 8주 로드맵 |

## 시작하기

```bash
npm install
npm test        # 판정 로직 59개 테스트
npm run dev
```

## 구조

```
src/
├── app/
│   ├── layout.tsx                          공통 레이아웃 + 메타데이터
│   ├── page.tsx                            허브
│   ├── sitemap.ts                          ★ 페이지 추가할 때마다 등록
│   ├── robots.ts
│   └── 건강보험/피부양자-자격판정/page.tsx    1순위 도구
├── components/
│   └── DependentJudge.tsx                  판정 폼 (클라이언트)
└── lib/
    ├── constants/
    │   ├── 2026.ts                         ★ 연도별 요건·요율
    │   └── property-score-table.ts          ⚠️ 미검증
    ├── dependent/
    │   ├── types.ts
    │   ├── judge.ts                        판정 로직 (순수 함수)
    │   └── judge.test.ts                   경계값 테스트
    └── premium/
        ├── regional.ts                     지역보험료 · 임의계속가입
        └── regional.test.ts
```

## 지켜야 할 규칙 3개

**1. 숫자를 코드에 직접 쓰지 않는다.**

요건과 요율은 매년 바뀝니다. 기존 계산기들이 낡아서 죽은 이유가 이것입니다.
모든 기준값은 `src/lib/constants/2026.ts`에 있고, 로직 파일에는 숫자 리터럴이 없습니다.

새 연도가 오면 `2026.ts`를 복사해 `2027.ts`를 만드세요.

**2. 로직을 UI에 넣지 않는다.**

`judge.ts`와 `regional.ts`는 순수 함수이고 React를 import하지 않습니다.
법령이 바뀔 때 컴포넌트를 뒤지게 되면 유지보수를 포기하게 됩니다.

**3. 결과에는 항상 근거와 기준 연도를 붙인다.**

이 사이트의 자산은 신뢰입니다. 모든 `StepResult`에 `basis`가 있고,
`JudgeResult`에 `year`가 있습니다. UI에서 빼지 마세요.

## 진행 상황

- [x] 판정 로직 + 경계값 테스트 59개
- [x] 지역보험료 · 임의계속가입 비교 계산
- [x] 판정 폼 UI, 허브, sitemap/robots, JSON-LD
- [ ] **재산점수 60등급표 확보** ← 보험료 페이지 공개 전 필수
- [ ] 지역가입자 보험료 페이지
- [ ] 임의계속가입 비교 페이지
- [ ] 가이드 6편
- [ ] 개인정보처리방침 · 이용약관 · 문의
- [ ] 애드센스 신청

## 다음 작업 (우선순위 순)

### 1. 재산점수 등급표 확보 — 가장 시급

`src/lib/constants/property-score-table.ts`의 구간표는 **근사값이고 틀렸습니다.**
공단 고시 60등급표로 교체하고 `VERIFIED = true`로 바꾸기 전까지
보험료 금액을 화면에 노출하지 마세요.

`calculateRegionalPremium()`이 `verified: false`를 반환하므로 UI에서 차단할 수 있습니다.
`regional.test.ts`에 이를 감시하는 테스트가 있습니다.

확인할 곳: 국민건강보험법 시행령 별표 4, 공단 "지역보험료 산정방법"

### 2. 배포

디자인을 다듬고 싶은 충동이 오더라도 **2주차에는 배포**하세요.
색인은 시간이 걸리고, 시계는 배포 시점부터 돕니다.

```bash
npx vercel
```

### 3. 서치콘솔 · 서치어드바이저 등록

배포 직후 바로. 구글과 네이버 둘 다 등록해야 합니다.

## 판정 요건 요약 (2026년)

| 요건 | 기준 |
|---|---|
| 합산소득 | 연 2,000만원 이하 (1원 초과도 탈락) |
| 사업소득 · 등록 있음 | 0원이어야 인정 |
| 사업소득 · 등록 없음 | 연 500만원 이하 |
| 재산 과표 | 5.4억 이하 인정 / 5.4~9억은 소득 1,000만원 이하 / 9억 초과 탈락 |
| 형제자매 재산 | 과표 1.8억 이하 |
| 형제자매 부양 | 만 30세 미만 또는 65세 이상 또는 장애인 + 미혼 + 동거 |
| 건강보험료율 | 7.19% |
| 장기요양보험료율 | 0.9448% |
| 재산점수당 금액 | 211.5원 |

근거: 국민건강보험법 시행규칙 별표 1, 별표 1의2 / 공단 2026년도 보험료율 고시

## 면책

본 프로젝트의 판정·계산 결과는 모의 결과이며 법적 효력이 없습니다.
최종 확인은 국민건강보험공단(1577-1000)에 문의해야 합니다.
